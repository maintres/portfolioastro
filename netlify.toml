[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Nota: Los archivos estáticos de public/ se sirven automáticamente
# El archivo _redirects en public/ maneja el routing SPA
